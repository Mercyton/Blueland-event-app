services:
  - type: web
    name: event-management-app
    env: node
    buildCommand: |
      bun install
      npx prisma generate
      npx prisma db push
      npm run db:seed
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: event-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true